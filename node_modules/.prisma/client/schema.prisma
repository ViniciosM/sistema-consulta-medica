generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Paciente {
  id               Int        @id @default(autoincrement())
  nome             String
  cpf              String     @unique
  dataNascimento   DateTime
  endereco         String?
  telefone         String?
  email            String?
  alergias         String?
  historicoClinico String?
  convenio         String?
  createdAt        DateTime   @default(now())
  updatedAt        DateTime   @updatedAt
  consultas        Consulta[]
}

model Profissional {
  id            Int        @id @default(autoincrement())
  nome          String
  crm           String     @unique
  especialidade String
  telefone      String?
  email         String?
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  consultas     Consulta[]
}

model Consulta {
  id             Int      @id @default(autoincrement())
  dataHora       DateTime
  status         String   @default("agendada") // agendada, cancelada, concluída, remarcada
  observacoes    String?
  pacienteId     Int
  profissionalId Int
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  paciente     Paciente     @relation(fields: [pacienteId], references: [id])
  profissional Profissional @relation(fields: [profissionalId], references: [id])

  prontuario Prontuario?
}

model Prontuario {
  id            Int          @id @default(autoincrement())
  consultaId    Int          @unique
  diagnostico   String
  condutaMedica String
  observacoes   String?
  createdAt     DateTime     @default(now())
  consulta      Consulta     @relation(fields: [consultaId], references: [id])
  prescricoes   Prescricao[]
}

model Prescricao {
  id           Int     @id @default(autoincrement())
  prontuarioId Int
  medicamento  String
  dosagem      String
  orientacoes  String?

  prontuario Prontuario @relation(fields: [prontuarioId], references: [id])
}

model Usuario {
  id        Int      @id @default(autoincrement())
  nome      String
  email     String   @unique
  senha     String
  perfil    String // operador, profissional, administrador
  createdAt DateTime @default(now())
}
